[project]
name = "ml-book"
version = "0.1.0"
description = "Probabilistic Machine Learning book notes"
readme = "README.md"
requires-python = ">=3.12"
dependencies = [
  "catppuccin-jupyterlab",
  "jupyterlab",
  "matplotlib",
  "numpy",
  "pandas",
  "plotly",
]
